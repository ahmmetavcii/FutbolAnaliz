# Football-tuned ByteTrack (Ultralytics)
# Longer buffer + slightly stricter new-track gate to reduce ID flips on motion.

tracker_type: bytetrack
track_high_thresh: 0.3
track_low_thresh: 0.1
new_track_thresh: 0.35
track_buffer: 90
match_thresh: 0.75
fuse_score: True
